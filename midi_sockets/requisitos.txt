# Paquetes Python (instalar con: pip install -r requisitos.txt)
mido>=1.3.2
mingus>=0.6.1

# NOTA: python-rtmidi solo es necesario si se usan puertos MIDI físicos.
# Para este proyecto se usa FluidSynth para síntesis de audio.

# DEPENDENCIA DEL SISTEMA (no es paquete Python):
# FluidSynth debe instalarse a nivel de sistema operativo:
#   Linux:   sudo apt-get install fluidsynth
#   macOS:   brew install fluid-synth
#   Windows: https://www.fluidsynth.org/download/
# Sin FluidSynth instalado, la reproducción de audio no funcionará
# aunque pip install sea exitoso.
